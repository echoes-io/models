/**
 * Re-export types from @echoes-io/utils to ensure compatibility
 * These are the source of truth for metadata and text statistics
 */
export type { ChapterMetadata, TextStats } from '@echoes-io/utils';
